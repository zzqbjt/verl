hydra:
  searchpath:
    - file://verl/trainer/config

defaults:
  - ppo_megatron_trainer
  - _self_

data:
  gen_batch_size: ${data.train_batch_size}

reward:
  reward_manager:
    name: dapo
  reward_kwargs:
    overlong_buffer_cfg:
      enable: False # We try to avoid forgetting to set enable
      len: 0
      penalty_factor: 0.0
      log: False
    max_resp_len: null

algorithm:
  filter_groups:
    _target_: verl.trainer.config.FilterGroupsConfig
    enable: False # We try to avoid forgetting to set enable
    metric: null # acc / score / seq_reward / seq_final_reward / ...
    max_num_gen_batches: 0 # Non-positive values mean no upper limit
    fill_shortfall: False # Fill from filtered-out prompts in order instead of generating another batch
    adaptive_topup:
      _target_: verl.trainer.config.AdaptiveTopupConfig
      enabled: False # Only extend initial groups with zero reward variance
      max_total_n: 0 # Maximum samples per topped-up prompt, including the initial rollout
      chunk_size: 4 # Extra responses generated for each pending prompt per round
      selection_seed: 42

trainer:
  project_name: verl-dapo
